# 阶段 5 验收与封板清单（按 9 条 MVP 验收项）

> 需求来源：`docs/MVP 版本功能描述（精简版0326）.md`  
> 执行日期：2026-03-29  
> 自动化验证命令：`scripts/phase5_validate.sh`

## 一、9条验收项核对

| # | 验收项 | 当前证据 | 结论 |
|---|---|---|---|
| 1 | 用户可正常创建、编辑、删除闹铃 | `AlarmListViewModelTests.testSaveAndDeleteAlarmCRUDFlow` | 自动化通过（待真机 UI 复核） |
| 2 | 闹铃响起前30分钟，手表端自动启动后台监测 | `AlarmListViewModelTests.testRuntimeDispatchSendsOnlyPrewarmAtMinusThirtyMinutes` + `WatchCommandRoutingTests.testPrewarmMessageStartsRuntimeSession` | 自动化通过（待真机时序复核） |
| 3 | 闹铃响起后，监测到清醒信号，5秒内自动静音 | `WatchSnoozeFeedbackTests.testAwakeSignalStopsRingingWithinFiveSeconds` | 自动化通过（待真机传感器复核） |
| 4 | 静音后5分钟内再次入睡自动重响 | `SmartAlarmEngineTests.testAsleepDuringMonitoringTriggersRering` | 逻辑通过（待真机5分钟链路复核） |
| 5 | 闹铃响起时手势触发贪睡，立即静音并X分钟后重响 | `WatchSnoozeFeedbackTests.testGestureSnoozeUpdatesStatusAndPlaysFeedback` + `SmartAlarmEngineTests.testSnoozeTimeoutReturnsToReringing` | 逻辑通过（待真机手势与计时复核） |
| 6 | 贪睡期间再次入睡仍按智能逻辑重响 | 同 #4 + #5 状态机链路覆盖 | 逻辑通过（待真机复核） |
| 7 | 传感器数据缺失时不漏叫 | `SensorDegradeTests.testPermissionMissingTriggersDegradedState` + `SmartAlarmEngineTests.testDegradeAlwaysKeepsRingingFallback` | 自动化通过（待真机场景复核） |
| 8 | 闹铃详情页可开关智能静音并设置贪睡间隔 | `AlarmListViewModelTests.testSaveAlarmPersistsSmartModeAndSnoozeSettings` | 自动化通过（待真机 UI 复核） |
| 9 | 权限申请流程完整，用户可正常授权 | `AlarmListViewModelTests.testRequestPermissionsShowsGuideWhenDenied` + `testPermissionGuideShowsReadyWhenGranted` | 自动化通过（待真机授权流程复核） |

## 二、自动化证据文件

- `artifacts/phase5/swift_test.log`
- `artifacts/phase5/ios_sim_build.log`
- `artifacts/phase5/watch_sim_build.log`

## 三、封板前清单（Release Gate）

## P0（必须通过）
- [ ] 真机 iPhone + Apple Watch 联调：完整跑通 9 条验收项并录屏。
- [ ] 关键时延复核：清醒检测后静音延迟 `< 5s`（含日志时间戳）。
- [ ] 防漏叫复核：传感器缺失、手表断连、运行会话失效时仍响铃。
- [ ] 权限流程复核：通知 + HealthKit 首次授权、拒绝后引导、二次恢复。
- [ ] 异常路径复核：低电量、未佩戴、蓝牙短断开恢复。

## P1（建议完成）
- [ ] 30分钟预热窗口与到点触发精度（跨天/重复闹铃）真机回归。
- [ ] 手势识别参数现场校准（误触发率与漏触发率日志统计）。
- [ ] 5分钟防再睡链路真机稳定性（连续多轮）。

## 四、当前阶段判定

- 当前自动化（单测 + 模拟器构建）已通过，可作为阶段 5 的代码与逻辑证据。
- 仍缺真机实测证据，因此发布判定为：`No-Go（待真机验收完成后转 Go）`。
